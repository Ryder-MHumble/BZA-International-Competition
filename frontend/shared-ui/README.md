# Shared UI

Shared front-end primitives and tokens for the public site and admin console.

Keep brand tokens here only when both front-end surfaces need them. Public-site-specific WebGL and editorial sections stay in `public-site`.
